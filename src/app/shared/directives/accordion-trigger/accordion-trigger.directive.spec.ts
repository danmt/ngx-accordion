import { AccordionTriggerDirective } from './accordion-trigger.directive';

describe('AccordionTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new AccordionTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
